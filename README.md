# Arafat Hossain — Portfolio

Personal portfolio website showcasing Arafat's skills, projects, and experience as a CSE student and aspiring software developer.

## Run locally

This is a dependency-free static site. Open `index.html` directly in a browser, or serve the repository with any static web server:

```text
python -m http.server
```

Then visit `http://localhost:8000`.
